version: '3.8'
services:
  # client:
  #   build:
  #     context: ./client
  #   ports:
  #     - "3000:3000"
  #   restart: always
  server:
    build:
      context: ./server
    ports:
      - "8000:8000"
    restart: always

